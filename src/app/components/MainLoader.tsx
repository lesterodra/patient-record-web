"use client";

import LoadingSpinner from "./LoadingSpinner";

const MainLoader = () => {
  return (
    <div className="center">
      <LoadingSpinner />
    </div>
  );
};

export default MainLoader;
